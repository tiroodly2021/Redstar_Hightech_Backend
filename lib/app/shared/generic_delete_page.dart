import 'package:flutter/material.dart';

class GeneriDeletePage extends StatelessWidget {
  const GeneriDeletePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
